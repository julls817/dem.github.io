
<html lang="ru">
<head>
    <title>Мой блог</title>
    <link type=“text/css” rel=“stylesheet” href=“style.css” />
</head>
<body>
    <blockquote class=“q”>Путешествие в тысячу миль начинается с одного шага (Лао Цзы) </blockquote>
    <h2> 5 декабря , 2025</h2>
    <img src=“Icons/icon.png" alt=“Новый год”>

    
<p>Поздравление с Наступающим 2026 годом читайте <a href=“Congratulations2026.html”> здесь.</a>  </p>
<h2> 15 октября , 2025</h2>
<p>
Я сделала Новогодние кроссворды для сайта <a href=“http://github.com”  title=“Блог моего друга”>Сайт моего друга</a>
</p>
<h2> 1 сентября , 2025</h2>
<blockquote>Путешествие в тысячу миль начинается с одного шага (Лао Цзы) </blockquote>
<p>
Оставляю в прошлом скучную неинтересную локальную файловую систему ради Всемирной паутины.
</p>
  <a href="https://www.flaticon.com/ru/free-icons/-" title="новый год иконки">Новый год иконки от Pixelmeetup - Flaticon</a>
</body>
</html>



# Доменное имя моего сайта

На текущую дату (13 января 2026 года) доменное имя моего сайта состоит из следующих частей:

- **Уровень 1 (TLD)**: `.com` (или `.ru`, `.org` и т.д. — зависит от вашего сайта)
- **Уровень 2 (SLD)**: `moy-sait` (например, если домен `moy-sait.com`)
- **Уровень 3 и выше**: `www` (если есть, например, `www.moy-sait.com`)

То есть полное доменное имя: `www.moy-sait.com`

*Примечание: Укажите реальное доменное имя вашего сайта, если оно есть. Если нет — придумайте пример.*


from transformers import AutoTokenizer, AutoModel
import torch
import numpy as np
from sklearn.metrics.pairwise import cosine_similarity

# 1) Загрузка токенизатора и модели RuBERT
model_checkpoint = 'DeepPavlov/rubert-base-cased'
tokenizer = AutoTokenizer.from_pretrained(model_checkpoint)
model = AutoModel.from_pretrained(model_checkpoint)
model.eval()

# 2) Функция для получения эмбеддингов (mean-pooling + L2-нормализация)
def embed(texts):
    enc = tokenizer(texts, padding=True, truncation=True, max_length=64, return_tensors="pt")
    with torch.no_grad():
        out = model(**enc).last_hidden_state
    emb = out.mean(dim=1).cpu().numpy()  # [batch, hidden]
    return emb / np.linalg.norm(emb, axis=1, keepdims=True)

# 3) Подготовка базы FAQ
faq_q = [
    "Как быстро доставляется заказ?",
    "Где гарантия на товар?",
    "Можно ли вернуть товар обратно?"
]
faq_a = [
    "Наш стандартный срок доставки — 3–5 рабочих дней.",
    "Гарантия 1 год, условия в документации.",
    "Да, возврат возможен в течение 14 дней."
]

# 4) Получение эмбеддингов для FAQ и запроса пользователя
emb_faq  = embed(faq_q)
user_q   = "Сколько ждать мой заказ?"
emb_user = embed([user_q])

# 5) Поиск наиболее похожего вопроса
sim      = cosine_similarity(emb_user, emb_faq)[0]
best_idx = int(sim.argmax())

# 6) Вывод результата
print("Вопрос пользователя:", user_q)
print("Найден FAQ-вопрос:", faq_q[best_idx], f"(sim={sim[best_idx]:.3f})")
print("Ответ бот-FAQ:", faq_a[best_idx])
